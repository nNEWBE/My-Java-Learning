public class Interface_vs_Abstract_Class {

    // ! Similarity
    // * (i) Interface can have any number of methods
    // ? (ii) It has the same file extension as class (.java)

    // ! Difference
    // * (i) You can not initialize a interface
    // ? (ii) Interface doesn't contain any constructor
    // do (iii) All methods in interface are abstract
    // @ (iv) An interface can't have instance variables
    // $ (v) An interface can extend multiple inheritance
    //// (vi) Abstract class is parallely abstract and interface is fully abstract
    // # (vii) Interface have only static and final variables but Abstract class has static, non-static, final, non-final variables

}
